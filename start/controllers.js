/** Load custom controllers **/

/*
 * By default, controllers will be loaded from the controller directory which be configured in /config/paths.js
 * Following this example to load custom controllers: 
 */
module.exports = {
    // "HomeController": __dir + "/controllers/home-controller",
};